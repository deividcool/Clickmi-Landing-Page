import type { Transformer } from '@remark-embedder/core';
declare type Provider = {
    provider_name: string;
    provider_url: string;
    endpoints: Array<{
        schemes?: string[];
        discovery?: boolean;
        url: string;
    }>;
};
declare type Config = {
    params?: {
        [key: string]: unknown;
    };
};
declare type GetConfig = ({ url, provider, }: {
    url: string;
    provider: Provider;
}) => Config | null | undefined;
declare const transformer: Transformer<Config | GetConfig>;
export default transformer;
declare type ExportedConfig = Config | GetConfig;
export type { ExportedConfig as Config };
